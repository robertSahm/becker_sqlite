$(".showcaseGuitar, .showcaseBass, .showcaseCustom").hide();
$(".showcaseTheme h2").html("<%= @msg %>");
$(".<%= @body_type.name %>").fadeIn(500);
//alert('<%= @body_type.id %>');
$('.spinnerTheme<%= @body_type.name %>').roundabout({
	//shape: 'tearDrop',
	//duration: 1000,
	minScale: .4, 
	maxScale: 1,
	focusBearing: 0,
	tilt: 3,
	minOpacity: .4,
	minZ: 279,
	maxZ: 280,
	easing: "swing",

});
//$(".showcaseTheme").fadeIn(500);



