$(".showcaseGuitar, .showcaseBass, .showcaseCustom").hide();
$("#messageReceiver").html("<%= @msg1 %>");
$("#choiceReceiver").html("<%= @msg2 %>");
$('#spinnerReceiver').html("<%=j render 'spinnerTheme', bt: @body_type %>");
$(".spinnerTheme").roundabout({
	//shape: 'tearDrop',
	//duration: 1000,
	minScale: .4, 
	maxScale: 1,
	focusBearing: 0,
	tilt: 3,
	minOpacity: .4,
	minZ: 279,
	maxZ: 280,
	easing: "swing",

});
